{
  "name": "trace-monorepo",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prebuild": "node node_modules/prisma/build/index.js generate --schema=apps/api/prisma/schema.prisma",
    "build": "chmod -R +x node_modules/.bin && node node_modules/turbo/bin/turbo run build",
    "dev": "npx turbo run dev",
    "lint": "npx turbo run lint",
    "test": "npx turbo run test"
  },
  "dependencies": {
    "turbo": "latest",
    "prisma": "^6.4.1"
  }
}
