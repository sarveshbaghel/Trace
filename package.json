{
  "name": "trace-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prebuild": "npx prisma generate --schema=apps/api/prisma/schema.prisma",
    "build": "npx turbo run build",
    "dev": "npx turbo run dev",
    "lint": "npx turbo run lint",
    "test": "npx turbo run test"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
